// import React, { useState } from "react";

// function Anagram({anagram, syn}) {

//     const [isFound, setIsFound] = useState(false)

//     const toRender = isFound ? <div className="found-syn">{syn}</div> : <div className="not-found-syn">{anagram}</div>

//     return toRender
//   }
  
//   export default Anagram;